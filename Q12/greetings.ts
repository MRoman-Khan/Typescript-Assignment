// Define an array called 'names' to store your friends' names
const names: string[] = ["Ali", "Waqas", "Zuhaib", "Roman"];

// Iterate through the 'names' array and print a personalized message to each person
for (const name of names) {
  console.log(`Hello, ${name}! Have a great day.`);
}
