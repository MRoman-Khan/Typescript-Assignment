// Store the famous person's name in a variable
const famous_person = "Albert Einstein";

// Define the quote by the famous person
const quote = "Imagination is more important than knowledge.";

// Compose the message including the quote and author
const message = `"${quote}"\n- ${famous_person}`;

// Print the message
console.log(message);
